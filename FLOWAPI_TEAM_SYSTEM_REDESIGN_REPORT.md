# FlowAPI 团队版 Token 管理系统验收报告

更新时间：2026-06-09

## 一句话结论

本次改造把 FlowAPI 从“个人 API Key 中转站”推进到“可灰度售卖的团队 Token 管理台”：队长可以创建团队、生成邀请、分配成员额度、创建团队 Key、查看成员消耗、导出团队账单；成员只能看到自己相关数据，团队调用会记录到团队日志、Token 池用量和团队账本流水。

但它还没有达到“完整企业版 8 分以上”的最终形态，因为团队钱包目前是团队对账层，真实扣费主账本仍沿用现有 FlowAPI 账户/API Key 余额结算链路。要做成真正企业闭环，下一步必须把“团队钱包作为唯一付款钱包”接进充值、套餐和扣费结算。

## 已完成范围

### 1. 团队空间入口

- 前台控制台顶部新增空间切换器：个人空间 / 团队空间。
- 左侧菜单修复团队空间与团队记账的高亮冲突。
- `/team` 页面升级为团队工作台，包含：
  - 团队总览
  - 成员额度
  - 团队 Key
  - 调用日志
  - 团队账单

### 2. 团队角色

保留 4 个小白能理解的角色：

- 团队队长：最高权限，可邀请、管理成员、设置额度、创建团队 Key、导出账单。
- 管理员：可管理普通成员和额度，但不能修改队长，不能扩散高权限角色。
- 财务：可看账单和导出账单，不可创建 Key 或改权限。
- 成员：只看自己的日志、额度和消耗。

### 3. 团队创建与邀请

- 新增 `POST /api/team/create`。
- 新增 `GET /api/team/list`。
- 新增 `POST /api/team/join`。
- 新增 `GET/POST /api/team/members`。
- 队长/管理员可生成邀请链接。
- 管理员只能邀请普通成员；财务/管理员等高权限角色必须由队长授予。
- 修改成员名称或状态时不会因为未传 `role` 而把管理员/财务误降级为普通成员。

### 4. 成员额度与限制

- 新增 `team_member_limits`。
- 支持按金额、Token、请求次数限制。
- 支持每日、每周、每月、总额度。
- 调用前会先检查成员额度，不足则直接返回，不请求上游。
- 超额失败会写入团队日志，状态为 `TEAM_MEMBER_QUOTA_EXCEEDED`。

### 5. 团队 API Key 权限

- 创建 API Key 时如果绑定团队，必须先验证操作者是否属于该团队且具备权限。
- 修复了任意用户通过 `teamId` 把 Key 绑定到别的团队的 P0 风险。
- 团队 Key 已绑定某个团队后，调用时不能再通过请求参数切到另一个团队，避免账本归属混乱。

### 6. Token 去向追踪

每次团队调用会记录：

- 谁调用：`user_id`
- 用哪个 Key：`api_key_id`
- 哪个团队：`team_id`
- 哪个模型：`model`
- 哪个上游/供应商：`provider`
- 请求 ID：`request_id`
- 输入、输出、总 Token
- 成本金额
- 成功/失败
- 延迟
- 错误码和错误信息

核心落表：

- `team_usage_logs`
- `token_pool_usage`
- `team_wallet_transactions`

### 7. 团队账单与导出

- `/api/team/billing` 改为团队系统账单，不再复用图片工作台的 workspace 账单。
- 新增 `GET /api/team/billing/export` 导出 CSV。
- 导出内容包含：
  - 团队总览
  - 成员消耗
  - 模型消耗
  - API Key 消耗
  - 失败请求

## 权限验收

| 场景 | 当前结果 |
| --- | --- |
| 未登录访问团队 API | 返回 401 |
| 普通成员查看团队账单 | 只能看到自己的日志/流水 |
| 队长查看团队账单 | 可看全员 |
| 财务导出账单 | 允许 |
| 成员导出账单 | 禁止 |
| 管理员修改队长权限 | 禁止 |
| 管理员邀请财务/管理员 | 禁止 |
| Key 绑定团队 A 后请求切团队 B | 禁止 |
| 成员超额度调用 | 不请求上游，失败不扣费 |
| 显式请求无权限团队 ID | 返回 403，不回退到第一个团队 |
| 普通成员访问团队日志接口 | 只能看到自己的日志 |

## 多角色评测会议

### CEO / 商业负责人

评分：7.2/10

优点：
- 已经能向工作室老板讲清楚“谁用了多少、哪个模型花钱最多、哪条请求失败”。
- 团队 Key 权限和成员额度开始具备商业销售价值。
- CSV 导出让人工对账可落地。

缺点：
- 团队钱包不是唯一付款钱包，还不能宣传为完整企业钱包。
- 团队套餐购买、团队充值、成员用量包尚未完全闭环。

修改意见：
- 下一阶段必须做“团队钱包充值 -> 团队 Key 扣团队余额 -> 成员消耗占用团队预算”的唯一账本。

### 产品经理

评分：7.4/10

优点：
- 创建团队、邀请成员、设置额度、查看账单这条路径已经形成。
- 页面文案从技术表述改成老板能看懂的业务语言。

缺点：
- 加入团队还缺少独立的邀请落地页。
- 创建 API Key 弹窗中选择团队和分配成员的流程还可以更傻瓜。

修改意见：
- 做 `/invite/team/:code` 页面。
- API Key 创建弹窗中增加“这个 Key 给谁用 / 归哪个团队账本”的明确选择。

### UI 设计师 / 小白体验官

评分：7.6/10

优点：
- 团队页现在像公司消费报表，不像技术后台。
- 指标卡、标签、表格信息层级清楚。
- 空间切换器位置合理，适合从个人版自然升级到团队版。

缺点：
- 成员额度编辑仍偏表格化，小白第一次看会有一点压力。
- 移动端表格需要后续做卡片化。

修改意见：
- 成员额度改成抽屉式编辑。
- 成员列表移动端显示“成员卡片”，减少横向滚动。

### 技术负责人 / 安全工程师

评分：7.1/10

优点：
- 修复了团队 Key 任意绑定团队的高危问题。
- 团队调用前有成员额度检查。
- 团队 Key 跨团队记账被拦截。
- 失败和超额会记录 request_id，方便客服排查。

缺点：
- 团队钱包与现有余额结算还不是同一个事务。
- 真实生产 E2E 需要使用管理员账号、团队成员账号和真实 API Key 做完整验证。

修改意见：
- 增加团队钱包 reserve/finalize/refund 事务层。
- 增加团队 API Key 分配给成员的强身份映射。

### 财务 / 运营负责人

评分：7.0/10

优点：
- 每笔团队调用已经能按成员、模型、API Key、request_id 对账。
- 可以导出 CSV，适合人工客服和小团队月度对账。

缺点：
- 目前团队余额不是真正扣款主账本，不能直接用于自动利润报表。
- 缺少团队套餐订单表、团队充值流水和团队发票/收据状态。

修改意见：
- 补 `team_orders` / `team_packages` / `team_recharge_orders`。
- 团队账单增加“用户售价、上游成本、毛利”。

### 路人小白测试

评分：7.5/10

优点：
- 能看懂“团队余额、总花费、谁最耗钱、哪个模型花钱最多”。
- 邀请成员和设置额度的入口明显。

缺点：
- 不知道“团队 Key”和“个人 Key”的区别，需要更明显的提示。
- 邀请链接生成后最好一键复制。

修改意见：
- 团队 Key 页面增加“老板统一充值，成员用团队 Key”的短说明。
- 邀请链接增加复制按钮和成功反馈。

## 当前综合评分

综合评分：7.3/10

可用结论：

- 可用于内部测试和小范围灰度售卖。
- 暂不建议直接宣传为“完整企业钱包版”。
- 如果要达到 8 分以上，需要完成团队钱包唯一账本、团队套餐订单、真实生产 E2E。

## 已验证命令

本地验证已通过：

- `npx eslint components/ConsoleLayout.js components/team/TeamSpaceConsole.js lib/customer-store.js lib/team-management.js lib/team-token-pool.js pages/api/v1/chat/completions.js pages/api/keys.js pages/api/team/overview.js pages/api/team/members.js pages/api/team/billing.js pages/api/team/list.js pages/api/team/create.js pages/api/team/join.js pages/api/team/billing/export.js`
- `git diff --check`
- `npm run build -- --webpack`
- `curl -I http://127.0.0.1:3003/team`
- `curl -s http://127.0.0.1:3003/api/team/list`
- `curl -s http://127.0.0.1:3003/api/health`

## 下一阶段必须做

1. 团队钱包作为唯一付款钱包：
   - 团队充值到账到 `team_wallets`。
   - 团队 Key 调用先 reserve 团队余额。
   - 成功 finalize，失败 refund。
   - 个人余额不再替团队 Key 付款。

2. 团队套餐订单：
   - `team_orders`
   - `team_packages`
   - `team_package_usages`
   - 支持周卡、月卡、团队包。

3. 成员身份和团队 Key 强绑定：
   - 队长可给某成员生成专属团队 Key。
   - 调用时日志记录真实成员，而不是只记录 Key 创建人。

4. 邀请落地页：
   - `/invite/team/:code`
   - 登录后自动加入团队。
   - 未登录先注册再加入。

5. 生产 E2E：
   - 队长账号创建团队。
   - 邀请成员账号加入。
   - 设置成员额度。
   - 生成成员团队 Key。
   - 真实调用模型消耗 Token。
   - 验证成员日志、团队账单、CSV 导出、超额拦截。
